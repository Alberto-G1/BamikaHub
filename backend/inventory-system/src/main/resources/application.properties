spring.application.name=inventory-system

# Server Configuration
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/bamikahub_is_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Grande123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# JWT Configuration
jwt.secret=6fb76cf5-b529-4e11-9539-f643f2ff65ad
jwt.expiration=86400000


# File Upload Configuration
# Use an absolute path outside the project folder for production-like behavior.
# On Windows, it might be: C:/bamikahub/uploads
# On Linux/macOS, it might be: /var/bamikahub/uploads
file.upload-dir=./uploads
# Add separate paths for different types of uploads
file.upload-dir.profile-pictures=${file.upload-dir}/profile-pictures
file.upload-dir.item-images=${file.upload-dir}/item-images
file.upload-dir.support-attachments=${file.upload-dir}/support-attachments

# Email/SMTP Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=nuwarindaalbertgrande@gmail.com
spring.mail.password=zqlideyvrgcgxbhz
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Notification Configuration
notifications.mail.enabled=true
notifications.mail.from=nuwarindaalbertgrande@gmail.com
#notifications.mail.escalation-recipients= nuwarindaalbertgrande@gmail.com

# Application Configuration
app.frontend.url=http://localhost:5173
app.company.name=Bamika Engineering
app.guest.magic-link.base-url=http://localhost:5173/guest/magic-login

# Scheduled Reports Configuration
reports.scheduled.enabled=false
reports.scheduled.recipients=admin@bamikahub.com,manager@bamikahub.com